<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListDemoActivity" android:label="ArrayList — רשימה" />
        <activity android:name=".MapDemoActivity" android:label="HashMap — מפה" />
        <activity android:name=".SetDemoActivity" android:label="HashSet — קבוצה" />
        <activity android:name=".ContactDemoActivity" android:label="המחלקה Contact" />
        <activity android:name=".SortDemoActivity" android:label="מיון אוספים" />
        <activity android:name=".PhoneBookActivity" android:label="ספר טלפונים" />
    </application>

</manifest>
